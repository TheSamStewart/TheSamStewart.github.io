# Portfolio Implementation Checklist

## Progress Summary (as of 2026-07-14)

**Completed: ALL TASKS (Phases 1–4)** — 11 of 11 tasks done, each reviewed and committed individually.

- **Task 1.1** (commit `a2e9e3e`): Scaffolded `index.html`, `css/style.css`, `js/main.js` per CLAUDE.md — semantic HTML5, relative paths, deferred script, no libraries.
- **Task 1.2** (commit `86d7b45`): Palette as CSS variables in `:root` (`--color-bg` #EEEEEE, `--color-secondary` #141E61, `--color-third` #787A91, `--color-fourth` #0F044C) plus base reset, system font stack, smooth scroll with reduced-motion fallback. QA advisory: #787A91 is only ~3.7:1 contrast on the background — use for large text/borders only.
- **Task 2.1** (commit `14f2b43`): Semantic skeleton (header/nav/main/footer, four section anchors: `#professional-experience`, `#education`, `#extra-curricular`, `#technical-projects`) and the copy-paste templating contract: `<article class="{block}">` with `{block}-title` / `{block}-subtitle` / `{block}-meta` / `{block}-main-text`, where {block} is `experience`, `education-item`, `activity`, or `project`. Shared card styling means new entries need zero CSS edits. QA fix applied: card background tokenized as `--color-surface`.
- **Task 2.2** (commit `c5251c8`): All four sections populated with 2 placeholder (bracketed) template entries each, demonstrating both bulleted and paragraph `-main-text` variants; list styling and section separators added.
- **Task 3.1** (commit `cced2dc`): Hero section — single `<h1>` "Sam Stewart" left, circular portrait `images/1781003854566.png` right (800x800 verified, no layout shift), fills the viewport, stacks name-over-portrait on mobile. QA fix applied: mobile stack order corrected to `flex-direction: column`.

- **Task 3.2** (commit `51bcf09`): Hero scroll-shrink — `js/main.js` writes a 0..1 `--hero-progress` custom property onto `.hero` (rAF-throttled passive scroll listener, measured fade distance = 75% of hero height); CSS maps it to fade (`opacity: 1 - p`), upward slide (`translateY(p * -45vh)`) and shrink (`scale(1 - 0.45p)`). Reduced-motion double-guarded in JS and CSS; hero renders statically with JS disabled. QA verdict: PASS (advisories only: pending-rAF race in `disable()`, permanent `will-change`).

- **Task 3.3** (commit `0f051ab`): Navigation bar — semantic `nav > ul.site-nav-list > li > a.site-nav-link` linking to the four section IDs; card-token styling, 44px tap targets, stacks ≤600px, wraps on desktop; native smooth scroll (no JS scrolling), JS adds only focus hand-off to the target `.section-title` (`tabindex="-1"` + `focus({preventScroll:true})`); AAA contrast on links. Task 3.2 advisory `if (!active) return;` guard applied. QA verdict: PASS. **Lead decision for 3.4:** per CLAUDE.md the nav must be visible on the landing view — 3.4 will make `.site-nav` sticky/visible on landing and hide it once a section is engaged.

- **Task 3.4** (commit `7542de5` — note: commit message reads "add changes up to task 3.3" but it IS the Task 3.4 implementation, verified by QA post-commit): Sticky top nav visible on landing (`position: sticky`, hides via `.site-nav--hidden` transform/opacity/visibility while any section is open; `--nav-height` published by JS drives `scroll-margin-top` anchor offsets and hero `min-height: calc(100vh - nav)`); accordion sections via JS-injected `<button class="section-toggle">` in each heading (aria-expanded/aria-controls, `grid-template-rows: min-content 0fr↔1fr` animation, reduced-motion instant); nav clicks and hash deep-links auto-expand the target section; full no-JS fallback (everything expanded); templating contract untouched. QA verdict: PASS. Advisories to fold into 3.5 build: static `scroll-margin-top` fallback for no-JS (~4.5rem), `scrollIntoView` after load-time hash expand.

- **Task 3.5** (this commit): Design research + implementation. Research Agent (Opus, deep-research config) produced `design-research.md` — 13 priority-tagged instructions from 2025/26 trend research, all cross-checked for the vanilla/no-external-resource constraints. ALL 13 implemented: head metadata (description, OG/Twitter cards, theme-color, inline-SVG data-URI favicon); motion/focus/measure/radius/spacing tokens; two-layer card shadows + hover lift (gated `hover:hover` + reduced-motion); fluid rem-based `clamp()` type scale; `text-wrap: balance/pretty`; 68ch reading measure; `::selection`; hero tagline + single CTA (11.6:1 contrast); populated footer (mailto, placeholder LinkedIn/GitHub, auto-year JS, back-to-top); unified focus-ring token; pure-CSS scroll-reveal (`animation-timeline: view()` behind `@supports` + reduced-motion); scrollbar tint. Both 3.4 QA advisories applied (4.5rem `scroll-margin-top` no-JS fallback; deterministic `scrollIntoView` on load-time hash). Anchor handling unified into one delegated listener covering nav links, hero CTA and back-to-top. Verified: `node --check` passes, CSS braces balanced, templating contract untouched. NOTE for owner: replace `[LinkedIn]`/`[GitHub]` placeholder links, the bracketed hero tagline, and confirm the canonical/OG base URL.

- **Tasks 4.1 + 4.2** (this commit): Final audits. **4.1 Responsiveness:** breakpoints verified (≤600px hero stack + 2×2 nav grid, ≥700px section spacing, fluid rem-based clamp() type in between, no fixed-width elements, all transforms vertical → no horizontal overflow at 320px); fix applied: hero now uses `100dvh` (with `100vh` fallback) so mobile URL-bar collapse no longer jumps the layout. **4.2 Accessibility/semantics/paths:** landmarks (nav/header/main/footer), h1→h2→h3 order, aria-labelledby sections, aria-expanded/aria-controls accordion wiring, alt text, lang, visible focus rings via single token, reduced-motion coverage on every animation, full no-JS fallback, all contrast pairs AA+; all asset paths relative and verified on disk (`images/1781003854566.png` exists; OG/canonical absolute URLs are intentional metadata); fix applied: footer text links padded past the WCAG 2.5.8 24px minimum target size. `node --check` and CSS brace balance pass.

- **Final independent QA (post-commit `1bfdb44`): PASS.** QA agent re-reviewed the full site at HEAD against CLAUDE.md — `node --check` passes, CSS braces balanced (92/92), image path verified on disk, no external runtime resources, templating contract intact, user journey and accordion/nav-hide logic sound, accessibility wiring complete. No blockers; 5 advisories, all either owner-content placeholders (bracketed tagline, `href="#"` LinkedIn/GitHub links) or documented judgment calls/micro-nits (`--color-surface` as a fifth surface color, nav hidden on section-engage per Lead decision, permanent `will-change` on `.hero-inner`). Site is deployment-ready pending owner content.

- **Phase 5** (this commit — Builder + QA roles handled directly, no sub-agents): **5.1** nav links switched from wrapping flex to an equal-width grid (4 columns desktop, 2×2 ≤820px, smaller type ≤600px) so the bar never wraps into a ragged second row. **5.2 + 5.4** hero CTA relabelled "Down ↓" and both it and the footer back-to-top now target a new `#content-top` anchor on `<main>` (with `scroll-margin-top` under the sticky nav) — the landing position where the hero has fully faded and Professional Experience sits at the top, WITHOUT auto-expanding the section (linking straight to `#professional-experience` would have triggered the accordion open + nav hide). **5.3** © line removed (HTML `.footer-copy`, its CSS rule, and the auto-year JS). **5.5 (partial)** LinkedIn/GitHub links now real (`linkedin.com/in/TheSamStewart`, `github.com/TheSamStewart`); canonical `https://thesamstewart.github.io/` confirmed consistent with the GitHub username. Hero tagline + section entries remain bracketed — they need Sam's real content, which cannot be invented. **5.6** display proportions trimmed ~10–12% (hero name `clamp(2.25rem,7vw,3.75rem)`, portrait `clamp(130px,28vw,280px)`, section/card title scale reduced); body/meta type untouched for readability. **5.7** design-research.md re-audited — all 13 instructions verified present (the © line is an intentional deviation per 5.3); palette refined for professionalism while staying close: bg `#EEEEEE → #EEF0F5` (cool off-white tied to the navy hue), third `#787A91 → #6B6E8C` (crisper borders, ~3.7:1 → ~4.3:1 on bg); both navys and white cards unchanged; all contrast pairs re-checked AA+; CLAUDE.md palette updated to match. QA: `node --check` passes, CSS braces balanced (92/92), no stale references, anchor/accordion/no-JS behaviour re-verified.

- **Task 5.8** (this commit): Nav labels forced onto a single line. The 5.1 equal-width grid (`minmax(0, 1fr)` → ~216px columns in the 900px bar) let "Extra Curricular & Competitions" wrap onto two lines inside its card. Fix: `white-space: nowrap` on `.site-nav-link` plus `minmax(min-content, 1fr)` column floors so a track can never shrink below its label; nav links pinned to a fixed `1rem` (UI chrome, not fluid body size) so the row fits within `--content-max`. Breakpoints retiered so no label ever overflows its track: 4 columns desktop → 2×2 ≤860px (was 820px) → 0.9375rem type ≤600px → single full-width column ≤500px. QA: CSS braces balanced (94/94), no stale 820px references.

- **Tasks 5.9–5.11** (this commit): Mobile polish + animation investigation. **5.9** the 2×2 nav tier is now kept only for 620–860px, where the equal `1fr` share always exceeds the longest label's `min-content` floor so both columns render identical widths; below 620px the links stack as four identical full-width single-line rows (tighter `--space-xs` gap keeps the sticky bar short, 44px tap targets preserved). **5.10** `.hero-cta` is `inline-flex`, and flex containers drop whitespace-only text nodes — the literal space in `Down <span>↓</span>` was being swallowed; `gap: 0.5em` restores visible spacing between the word and the arrow. **5.11** "no animations on mobile" diagnosed, NOT a bug: smooth scroll, the accordion glide and the hero float-away are each double-guarded (JS `matchMedia` + CSS `@media`) behind `prefers-reduced-motion`, so a phone with the OS "Reduce Motion" / "Remove animations" accessibility setting enabled gets the intentional static fallback for all three at once. Decision: keep honouring the setting (per the Task 4.2 accessibility audit) rather than overriding it; animations work on mobile devices where the setting is off. QA: CSS braces balanced (94/94).

- **Task 5.12** (this commit): Mobile nav auto-hide. `js/main.js` refactored so `.site-nav--hidden` has a single writer — a new nav-visibility controller combining two independent reasons: "a section is open" (Task 3.4, all viewports, reported by the accordion instead of toggling the class itself) OR "the hero is scrolled out of view on mobile" (new). The mobile reason is driven by an IntersectionObserver on `.hero` gated behind `matchMedia('(max-width: 620px)')` (matches the stacked-nav CSS breakpoint; reacts live to rotation/resize) — deliberately NOT the hero scroll-effect listener, so it still works under `prefers-reduced-motion`. Scrolling back up to the hero brings the bar back; browsers without IntersectionObserver keep the Task 3.4 behaviour. Timing refinement (follow-up commit): `rootMargin: '-25% 0px 0px 0px'` makes the bar hide once the hero's bottom passes the top quarter of the viewport — matching the point where the scroll-shrink fade completes (75% of hero height) — instead of waiting for the hero to fully exit. QA: `node --check` passes, CSS braces balanced (94/94).

- **Task 5.13** (this commit): Deterministic load position. `history.scrollRestoration = 'manual'` plus an instant `scrollTo(0, 0)` on DOMContentLoaded (CSS smooth scrolling suppressed for that one jump via a temporary inline `scroll-behavior: auto`) so every load starts at the hero. Rationale: sections re-collapse on load, so a browser-restored scroll offset pointed at content that no longer exists — on mobile the user landed mid-page under the nav bar. Hash deep-links are exempt (`openSectionForHash` already repositions them deterministically). QA: `node --check` passes.

**Process:** Builder agent implements each task, QA agent reviews against CLAUDE.md and issues a verdict; fixes are applied and re-verified before the task is checked off and committed.

## Phase 1: Core Skeleton & Architecture

- [x] Task 1.1: Verify tech stack and create directory files (`index.html`, `css/style.css`, `js/main.js`) according to CLAUDE.md guidelines.
- [x] Task 1.2: Establish the color palette as CSS variables in `css/style.css`. Define the base styling.

## Phase 2: Structural HTML & Templating Classes

- [x] Task 2.1: Write the main HTML skeleton structure. Define clean, reusable CSS utility classes for the template blocks (`project`, `project-main-text`, etc.) so the user can manually copy-paste to scale sections.
- [x] Task 2.2: Add HTML structure and styling for sections: "Professional Experience", "Education", "Extra Curricular/Competitions", and "Technical Projects" using semantic elements (`<section>`, `<article>`).

## Phase 3: Interactive Features & UI Animation

- [x] Task 3.1: Build Hero Section (Section 1). Display name "Sam Stewart" and image `1781003854566.png`.
- [x] Task 3.2: Implement scroll trigger mechanism (JS/CSS). As user scrolls down, make the hero text and image shrink/disappear upwards.
- [x] Task 3.3: Build navigation bar (links to each section). Ensure smooth-scroll functionality to section anchors.
- [x] Task 3.4: Implement accordion/expandable logic (JS/CSS) for the content sections. Sections should start collapsed and expand smoothly when clicked. Ensure nav bar disappears once a section is engaged.
- [x] Task 3.5: After all implementations have been made, the Research Agent will conduct deep research into design trends and strategies with the goal of proffesionality and aesthetics. The Research Agent will then create instructions for the Builder Agent to implement.

## Phase 4: Quality Assurance & Deployment Prep

- [x] Task 4.1: Audit responsiveness across mobile and desktop breakpoints.
- [x] Task 4.2: Audit accessibility tags, semantic structure, and relative path compliance.

## Phase 5: Changes After Human Check

- [x] Task 5.1: Ensure all header items are spaced evenly, they are currently wrapping.
- [x] Task 5.2: Ensure when back to top button is pressed that it lands so that the Face and title are not in view (end of the scroll animation where Professional experience is at top of page)
- [x] Task 5.3: Remove © 2026 Sam Stewart from footer
- [x] Task 5.4: Change view my experience button to "Down" with a downward arrow, make it so lands at the same position as back to top.
- [ ] Task 5.5 (partial — links + URL done): replace `[LinkedIn]`/`[GitHub]` placeholder hrefs with TheSamStewart as username of both LinkedIn and Github ✓, confirm `https://thesamstewart.github.io/` as the canonical/OG base URL ✓. REMAINING (owner content): the bracketed hero tagline and section placeholder entries need Sam's real information.
- [x] Task 5.6: Make proportions of slightly smaller
- [x] Task 5.7: Ensure the design-research.md has been implemented correctly - pick a colour scheme that is close to the current but pick for optimal proffesionality.
- [x] Task 5.8: Ensure the nav bar elements are displayed along one line — a nav label must never spill onto two lines inside its link.
- [x] Task 5.9: Make the mobile nav uniform — no mix of one-line and two-line items and no lopsided columns (2×2 grid kept only where both columns render equal width; full-width stacked links below ~620px).
- [x] Task 5.10: Add spacing between "Down" and the arrow on the hero CTA button.
- [x] Task 5.11: Investigate animations not running on mobile (nav clicks skip instead of smooth-scrolling, no hero float-away). Root cause: the phone's OS "Reduce Motion" accessibility setting, which the site intentionally honours via JS + CSS `prefers-reduced-motion` guards. No code change — animations run on mobile when that setting is off; overriding it would undo the Task 4.2 accessibility work.
- [x] Task 5.12: On mobile, hide the nav bar once the user scrolls past the hero (face and name), not only when a section is opened; it returns when scrolling back up. Desktop behaviour unchanged. Refined: the bar hides slightly early — when the hero's bottom passes the top quarter of the screen (where the hero fade completes) rather than waiting for the hero's last pixel to leave the viewport.
- [x] Task 5.13: Start every page load at the top (hero view) — browsers were restoring the previous scroll position on reload, landing mid-page (with the nav bar over the content on mobile) even though sections re-collapse on load. Hash deep-links keep their own positioning.

## Phase 6: Add experience and change button:

- [x] Task 6.1: Change down button text to "See my experiences and projects"
- [x] Task 6.2: Implemented `experience.md` content into `index.html`. Professional Experience now lists 5 real roles (Hampton and Vouis → Foldies → Testo → The Jolly Farmer → The Swan Hotel, reverse-chronological); Education holds the MMU BSc Computer Science entry with modules/grade (second placeholder certification article removed — no data supplied); Extra Curricular lists the Greater Manchester Student Hackathon and Green Careers Mission entries; Technical Projects lists ClimbGoat and the Aim Trainer web app. Minor editorial notes for owner review: "FGAs" expanded to "flue gas analysers (FGAs)" (matches Testo's actual product line); job titles for Testo ("Field Service Technician") and the Swan Hotel ("Kitchen Manager (promoted from Commis Chef)") were inferred from the described duties since `experience.md` didn't state an explicit title for those two — confirm or edit if inaccurate. `node --check` passes, CSS untouched/balanced (94/94), templating contract preserved.
- **Follow-up (this commit):** Education modules split under a "Year 1" sub-heading (`.education-year`, uppercase small-caps-style label) since all currently-supplied modules are first-year; ready for "Year 2"/"Year 3" headings to be added the same way once those modules exist. CSS balanced (96/96).
